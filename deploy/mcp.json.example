{
  "mcpServers": {
    "example": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-everything"],
      "env": {
        "API_TOKEN": "set-this-on-the-host-not-in-a-snapshot-pack"
      }
    }
  }
}
